<dialog class="detail-modal" id="detail-modal" aria-labelledby="detail-title" aria-describedby="detail-description">
  <div class="modal-frame">
    <button class="modal-close" type="button" aria-label="Close details">
      <span aria-hidden="true"></span><span aria-hidden="true"></span>
    </button>
    <div class="modal-visual" aria-hidden="true">
      <div class="modal-object"><span></span><i></i><b></b></div>
    </div>
    <div class="modal-copy">
      <p class="eyebrow" id="detail-eyebrow">Selected detail</p>
      <h2 id="detail-title"></h2>
      <p id="detail-description"></p>
      <dl class="modal-meta"></dl>
      <a class="modal-action" href="#" target="_blank" rel="noreferrer">View project <span aria-hidden="true">↗</span></a>
    </div>
  </div>
</dialog>

<script>
  import { setupDetailModal } from "../lib/detail-modal";
  setupDetailModal();
</script>

<style>
  .detail-modal { width: min(58rem, calc(100vw - 2rem)); max-height: min(47rem, calc(100vh - 2rem)); padding: 0; border: 1px solid rgba(32,33,33,.14); border-radius: clamp(1.4rem, 3vw, 2.4rem); background: rgba(253,253,252,.97); color: var(--ink); box-shadow: var(--shadow-float); opacity: 0; transform: scale(.965) translateY(.4rem); transition: opacity .28s ease, transform .38s cubic-bezier(.22,.8,.22,1), display .38s allow-discrete, overlay .38s allow-discrete; overflow: clip; }
  .detail-modal[open] { opacity: 1; transform: none; }
  @starting-style { .detail-modal[open] { opacity: 0; transform: scale(.965) translateY(.4rem); } }
  .detail-modal::backdrop { background: rgba(32,34,35,.18); backdrop-filter: blur(13px) saturate(.75); opacity: 0; transition: opacity .32s ease, display .32s allow-discrete, overlay .32s allow-discrete; }
  .detail-modal[open]::backdrop { opacity: 1; }
  @starting-style { .detail-modal[open]::backdrop { opacity: 0; } }
  .modal-frame { display: grid; grid-template-columns: 1.04fr .96fr; min-height: 34rem; }
  .modal-close { position: absolute; top: 1.25rem; right: 1.25rem; z-index: 2; display: grid; place-content: center; width: 2.75rem; height: 2.75rem; border: 1px solid rgba(32,33,33,.12); border-radius: 50%; background: rgba(253,253,252,.78); backdrop-filter: blur(12px); cursor: pointer; transition: transform .2s ease, background .2s ease; }
  .modal-close:hover { transform: rotate(3deg); background: white; }
  .modal-close span { grid-area: 1/1; width: 1rem; height: 1px; background: var(--ink); transform: rotate(45deg); }
  .modal-close span:last-child { transform: rotate(-45deg); }
  .modal-visual { --modal-accent: #c8cbcd; display: grid; place-items: center; min-height: 100%; background: linear-gradient(145deg, color-mix(in srgb, var(--modal-accent), white 28%), color-mix(in srgb, var(--modal-accent), white 76%)); border-right: 1px solid rgba(32,33,33,.08); }
  .modal-object { position: relative; width: min(72%, 18rem); aspect-ratio: 1.2; border: 1px solid rgba(32,33,33,.16); border-radius: 1.7rem; background: linear-gradient(145deg, rgba(255,255,255,.82), rgba(225,226,224,.78)); box-shadow: 0 24px 38px rgba(30,32,33,.17), inset 0 1px 0 white; }
  .modal-object span { position: absolute; left: 1.4rem; top: 1.4rem; width: 2.25rem; aspect-ratio: 1; border-radius: .65rem; background: var(--ink); }
  .modal-object i, .modal-object b { position: absolute; bottom: 1.4rem; width: 4.3rem; aspect-ratio: 1; border-radius: 50%; background: radial-gradient(circle at 38% 32%, #fff 0 8%, #d5d6d4 18%, #8e9191 50%, #dfe0de 52%, #aaa 60%, #ececea 61%); box-shadow: 0 8px 12px rgba(0,0,0,.18); }
  .modal-object i { right: 6.4rem; }.modal-object b { right: 1.35rem; }
  .modal-copy { align-self: center; padding: clamp(3rem, 6vw, 5.2rem); }
  .modal-copy h2 { margin-top: 1.15rem; font-size: clamp(2rem, 4vw, 3.35rem); line-height: .98; letter-spacing: -.055em; font-weight: 540; }
  #detail-description { margin-top: 1.5rem; color: #5e6060; line-height: 1.65; font-size: .98rem; }
  .modal-meta { display: grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap: 1.3rem; margin-top: 2rem; padding-top: 1.4rem; border-top: 1px solid var(--line); }
  .modal-meta :global(div) { min-width: 0; }
  .modal-meta :global(dt) { margin-bottom: .3rem; font: .62rem var(--font-mono); letter-spacing: .08em; text-transform: uppercase; color: var(--muted); }
  .modal-meta :global(dd) { margin: 0; font-size: .8rem; }
  .modal-action { display: inline-flex; align-items: center; justify-content: space-between; gap: 1rem; min-width: 10.5rem; margin-top: 2.2rem; padding: .85rem 1rem; border-radius: 999px; background: var(--ink); color: white; font-size: .82rem; transition: transform .2s ease; }
  .modal-action:hover { transform: translateY(-1px); }
  @media (max-width: 720px) { .detail-modal { overflow: auto; } .modal-frame { grid-template-columns: 1fr; } .modal-visual { min-height: 15rem; border-right: 0; border-bottom: 1px solid rgba(32,33,33,.08); } .modal-copy { padding: 2.2rem 1.5rem 2rem; } .modal-object { width: min(58%, 13rem); } }
</style>
